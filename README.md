Correr el front
cd ~/my-monorepo/web-app && npm run dev

Correr el backend
cd ~/my-monorepo/server && npx tsx watch src/index.ts

Y este no recuerdo
cd ~/my-monorepo/server && npm install -D typescript @types/node @types/express tsx && npx tsc --init && echo "¡Backend reparado con TypeScript con éxito!"

